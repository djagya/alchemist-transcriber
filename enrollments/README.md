# Enrollment clips (local only)

Place **reference audio** for each role here so diarization labels map to names via WeSpeaker:

- `danil.wav` (or `.m4a`, `.mp3`, `.flac`, `.ogg`)
- `therapist.*` with the same extensions

These files are **gitignored**; do not commit voice samples to the repository.
